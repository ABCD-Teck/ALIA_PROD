name: Translate Missing Chinese Titles

on:
  # 仅手动触发
  workflow_dispatch:
    inputs:
      limit:
        description: '翻译文章数量上限'
        required: false
        default: '50'

jobs:
  translate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run translation script
        id: translate
        env:
          PGHOST: ${{ secrets.PGHOST }}
          PGPORT: ${{ secrets.PGPORT }}
          PGDATABASE_MIA: ${{ secrets.PGDATABASE_MIA }}
          PGUSER: ${{ secrets.PGUSER }}
          PGPASSWORD: ${{ secrets.PGPASSWORD }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          node scripts/database/batch_translate_titles.js --limit ${{ github.event.inputs.limit || '50' }}

      - name: Summary
        if: always()
        run: |
          echo "## 📊 市场洞察标题翻译报告" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 执行信息" >> $GITHUB_STEP_SUMMARY
          echo "- **执行时间:** $(date -u '+%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
          echo "- **翻译数量上限:** ${{ github.event.inputs.limit || '50' }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ -n '${{ steps.translate.outputs.stats }}' ]; then
            STATS='${{ steps.translate.outputs.stats }}'
            echo "### 翻译统计" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "| 指标 | 数量 |" >> $GITHUB_STEP_SUMMARY
            echo "|------|------|" >> $GITHUB_STEP_SUMMARY
            echo "| 缺少中文标题总数 | $(echo $STATS | jq -r '.total_missing') |" >> $GITHUB_STEP_SUMMARY
            echo "| 本次处理数量 | $(echo $STATS | jq -r '.processed') |" >> $GITHUB_STEP_SUMMARY
            echo "| ✅ 成功翻译 | $(echo $STATS | jq -r '.translated') |" >> $GITHUB_STEP_SUMMARY
            echo "| ⏭️ 跳过(已是中文) | $(echo $STATS | jq -r '.skipped') |" >> $GITHUB_STEP_SUMMARY
            echo "| ❌ 错误 | $(echo $STATS | jq -r '.errors') |" >> $GITHUB_STEP_SUMMARY
            echo "| 📋 剩余未处理 | $(echo $STATS | jq -r '.remaining') |" >> $GITHUB_STEP_SUMMARY
          fi
